// This is the chatlog list of messages between an agent and their client
import React from 'react';

class ChatList extends React.Component {
  render() {
    return (
      <div> This is the ChatList component! </div>
    );
  }
}

export default ChatList;
