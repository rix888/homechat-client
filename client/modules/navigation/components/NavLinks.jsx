// This is the links on the left side, Holmes, Leads, Conversations, and Hooks
// should all go under here since most of them are going to trigger state and
// not implement a ton of logic themselves
import React from 'react';

class NavLinks extends React.Component {
  render() {
    return (
      <div> This is the NavLinks component! </div>
    );
  }
}

export default NavLinks;
