import React from 'react';

class ProfileSettings extends React.Component {
  render() {
    return (
      <div> This is the ProfileSettings component! </div>
    );
  }
}

export default ProfileSettings;
